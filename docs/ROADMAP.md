# Roadmap

## Completed Baseline

- Stabilize local calculator behavior.
- Keep online AI optional and non-persistent for secrets.
- Add release hygiene, packaging metadata and documentation baseline.
- Extract natural-query preview generation and query-help suggestions from `calculator.py`.
- Add install test in a clean virtual environment.
- Add GUI smoke checks for light and dark themes.
- Introduce a packaged Linux distribution path.
- Add screenshot-based UI regression checks.
- Review privacy notes and support process with actual pilot users.

## Release 1.x

- Keep Flatpak, AppStream, changelog and package metadata aligned for each release.
- Expand screenshot smoke coverage for calculator, assistant and settings states.
- Continue reducing UI duplication by keeping shared styles in `ui/theme.py` and shared button metadata in `ui/button_config.py`.
- Review privacy notes and support process after real user feedback.
