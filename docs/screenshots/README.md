# Screenshot Placeholders

Flathub validation expects real screenshot URLs in the AppStream metadata.

Add at least these two files before submitting to Flathub:

- `matrix-calculator-graphite.png`
- `matrix-calculator-green.png`

The current metainfo file references them through the GitHub raw URLs under this directory.
